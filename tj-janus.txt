Link to finished code pen: https://codepen.io/TJanus/pen/XWdWJrb

 1. What is Semantic HTML? 
    2. What is HTML used for? 
    it's used for the structure of my website
    3. What is an attribute and where do we put it? 
    HTML elements can have attributes.  An attribute provides more info about the element.  Example: <a is a tag... <a href (href is the attribute)
    4. What is the h1 tag used for? How many times should I use it on a page?
    h1 is heading1 which should be the largest heading on the page and only used once.  
    5. Name two tags that have required attributes
    <a requires href --- <img requires src
    6. What do we put in the head of our HTML document? 
    <head> tag which is used for metadata 
    7. What is an id? 
    An ID can only be used once on a document. (id="new")
    8. What elements can I add an id to? 
    base, head, html, meta,script, title
    9. How many times can I use the same id on a page?
     only once
    10. What is a class? 
    A class is when you are naming an element, mostly used to point to a class on a CSS file.
    11. What elements can I add a class to? <div> <section> <p> <h1> etc. -- <div class="newdiv">
    12. How many times can I use the same class on a page? no limit
    13. How do I get my link to open in a new tab? target="_blank"
    14. What is the alt attribute in the image tag used for? to describe the image with text, for screenreaders etc.
    15. How do I reference an id? using a #
    16. What is the difference between a section and a div? div is a block level element, while section is a semantic tag which gives information.  
    17. What is CSS used for? styling our html 
    18. How to we select an element? Example - every h2 on the page h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    id="nav" this lets us select that specific list of navigation and not all of them on the page.  That would be a class, class="nav" could select every class with nav in it/.
    20. How do we select classes in CSS? we use a period .classname
    21. How do we select a p element with a single class of “human””? .human
    22. What is a parent child selector? When would this be useful? its used to select all elements that are a direct child of the parent.  This is nice for lining boxes up vertically or horizontally.
    23. How do you select all links within a div with the class of sidebar?  .sidebar a
    24. What is a pseudo selector? A pseudo selector is like the :hover selector.  It specifies the state of the selected element. 
    25. What do we use the change the spacing between lines? line-height property
    26. What do we use to change the spacing between letters? letter-spacing property
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform: uppercase,lowercase, ;
    28. How do I add a 1px border around my div that is dotted and black? div { border: 1px dotted black;}
    29. How do I select everything on the page? * {}
    30. How do I write a comment in CSS? /* this is a comment */
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?  git rm - have to be careful because it will permantely delete it. 
    34. Why should I use version control? It helps developers/students that work in teams efficiently, and prevents code from breaking, getting lost etc.
    35. How often should I commit to github? every 20 minutes, or after a major feature starts working, or at the end of the day. 
    36. What is the command we would use to push our repo up to github? git push -u origin tj-janus
    37. Walk me through Lambda's git flow. Fork from Git, add TL as collaborator, Clone, copy link, paste it, CD into correct folder, git checkout/branch, git add.
    git commit -m "message", git push -u origin tj-janus,create a pull request, and add your TL as a reviewer.  

Stretch Questions

    1. What is the difference between an inline element and a block element? inline elements do not force a new line to begin in the doc.  Block elements
    cause a line to break. 
    2. What happens when an element is positioned absolutely? If positioned absolutely that element will never move when the page is scrolling, it's static.  
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
